/node_modules
/.next
/out
next-env.d.ts
*.tsbuildinfo
.DS_Store

# Leftover data dirs from the old watch-folder service. They hold real personal
# documents on this machine — this repo is published to a PUBLIC GitHub Pages
# site, so these must never be committed. The web app does not use them.
/inbox/
/dist/

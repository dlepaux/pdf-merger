# Optional. Set to a Google Analytics 4 measurement id (e.g. G-XXXXXXXXXX) to
# enable cookieless page-view analytics. Leave unset to bundle no analytics.
# In production this is provided by the GitHub Actions repo variable of the
# same name; copy this file to .env.local to enable it locally.
NEXT_PUBLIC_GA_ID=

{
  "name": "pdf-merger",
  "version": "0.1.0",
  "description": "Watch-folder PDF merge service — drop PDFs in inbox/, get a combined dist/merged.pdf",
  "type": "module",
  "private": true,
  "engines": {
    "node": ">=22.18"
  },
  "scripts": {
    "start": "node src/index.ts",
    "test": "node --test src/merge.test.ts src/service.test.ts",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "chokidar": "^4.0.3",
    "pdf-lib": "^1.17.1"
  },
  "devDependencies": {
    "@types/node": "^22.15.0",
    "typescript": "^5.8.0"
  }
}
